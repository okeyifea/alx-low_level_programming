#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen_recursion - check the code
 * @s: length of string
 * Return: Always 0
 */
int _strlen_recursion(char *s)
{
	char str[]= "_strlen_recursion";
	printf("Length of string is: %d", strlen(str));
	return 0;
}
