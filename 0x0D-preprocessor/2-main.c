#include "main.h"
#include <stdio.h>

/**
 * main - print the name of the file compiled
 * from, followed by a new line
 *
 * Return: Always 0
 */

int main(void)
{
	printf("%s\n", _FILE_);
	return (0);
}
