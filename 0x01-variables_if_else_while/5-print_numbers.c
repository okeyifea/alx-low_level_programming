#include <stdio>
/**
 * main - Entry point
 *
 * Return 0 (success/correct)
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
	printf("%digit", digit);
	}

	printf('\n');

	return (0);
}
